# Optional environment for official DOFA inference.
# Keep this separate from the base smoke-test install.

torch
torchvision
# DOFAv2's author-maintained ViT-B/14 implementation requires timm>=1.0.15.
# Freeze the exact version used by the 194/194-key checkpoint compatibility test.
timm==1.0.15
kornia
numpy>=1.24,<2.1
PyYAML
datasets
huggingface_hub
pillow
h5py

# Optional for converting or reading geospatial chips:
rasterio

# Notes:
# - Python 3.10+ is recommended for this project.
# - The base tests are CPU-safe and do not require this file.
# - Linux or Colab is recommended for real checkpoint inference.
# - Windows may work for torch.hub/local repo inference, but geospatial wheels
#   such as rasterio are often easier to manage on Linux/conda.
